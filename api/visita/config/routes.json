{
  "routes": [
    {
      "method": "GET",
      "path": "/visitantes",
      "handler": "visitante.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/visitantes/count",
      "handler": "visitante.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/visitantes/:id",
      "handler": "visitante.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/visitantes",
      "handler": "visitante.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/visitantes/:id",
      "handler": "visitante.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/visitantes/:id",
      "handler": "visitante.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
